<?php

/**
 * @group Lib
 */

class ProviderTest extends CIUnit_TestCase
{
	private $provider;
	public function setUp()
	{
		// Set up fixtures to be run before each test
		
		// Load the tested library so it will be available in all tests
		//$this->CI->load->library('example_lib', '', mylib);
	//	$provider = '';
	//	$this->CI->load->library('providers', '', $provider);
	}
	
	public function testRequestComdep()
	{
		// Check if everything is ok
		//$this->assertEquals(4, $this->CI->mylib->add(2, 2));
	/*	$comdep = new $this->CI->provider->RequestProvider();
		$result = $comdep->Cargar_Comdep();
		$this->AssertTrue($result);*/
	}
}
